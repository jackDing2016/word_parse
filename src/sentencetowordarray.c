#include <stdio.h>

int main(){

	char *sentence = "here we go";

	char *wordList[ 20 ];

	int wordListIndex = 0;

	char wordToSave;

	while ( *sentence != '\0' ) {
		printf("%c", *sentence);
		sentence++;
	}


}
